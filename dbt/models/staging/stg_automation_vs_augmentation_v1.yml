models:
  - name: stg_automation_vs_augmentation_v1
    description: "Data on automation/augmentation patterns from the first Anthropic Economic Index report"
    columns:
      - name: interaction_type
        data_type: string
        description: "Type of interaction (directive, feedback loop, validation, task iteration, learning, none)"

      - name: pct
        data_type: float64
        description: "Percentage of this interaction type in the dataset"